When updating the project
- Check if README.md in trickle/notes needs updating
- Update component list if new components added
- Update features if functionality changes
- Keep documentation in sync with code